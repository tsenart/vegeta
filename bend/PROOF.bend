# Vegeta in Bend -- the proofs. Each law L.x of LAWS.bend is proven by a
# def named L.x here. Proofs stay symbolic: a step never computes with a
# closed Nat above 10^6 (theory Nats are unary; see README).
import Base
import ./LAWS.bend as L
