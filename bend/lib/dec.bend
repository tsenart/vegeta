# Decimal codec for Nat. STUB: signatures only; LAWS.bend states the behavior.
import Base

def show(n: Nat) -> String:
  "0"

def read(s: String) -> Maybe<&2, Nat>:
  None{}

def pad(w: Nat, n: Nat) -> String:
  "0"
