language: go
sudo: false
go:
  - tip
install:
  - go get -v golang.org/x/lint/golint
  - go get -d -t -v ./...
  - go build -v ./...
script:
  - go vet ./...
  - $HOME/gopath/bin/golint -set_exit_status $(go list ./... | grep -v /vendor/)
  - go test -v ./...
